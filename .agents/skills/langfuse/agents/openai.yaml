interface:
  display_name: "Langfuse"
  short_description: "Repo-specific patterns and workflows for langfuse"
  default_prompt: "Use the langfuse repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true